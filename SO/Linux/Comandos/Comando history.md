Muestra un historial de los comandos que se han ejecutado en la terminal. Cada comando tiene un número de referencia.

```bash
history
```

Si queremos ejecutar el comando 38 del listado, haríamos:

```bash
!38
```
